# Core dependencies
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
opencv-python>=4.8.0
Pillow>=10.0.0

# Face detection and recognition
facexlib>=0.3.0
retinaface-pytorch>=0.0.8
insightface>=0.7.3

# Image enhancement and super-resolution
basicsr>=1.4.2
gfpgan>=1.3.8
codeformer>=0.1.0
real-esrgan>=0.3.0

# Computer vision utilities
scikit-image>=0.21.0
imageio>=2.31.0
imageio-ffmpeg>=0.4.9

# CLI and UI
click>=8.1.0
rich>=13.5.0
tqdm>=4.66.0
typer>=0.9.0

# API server
fastapi>=0.103.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Configuration
pyyaml>=6.0
python-dotenv>=1.0.0
omegaconf>=2.3.0

# Logging and monitoring
loguru>=0.7.0
prometheus-client>=0.17.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
httpx>=0.24.0

# Code quality
black>=23.7.0
flake8>=6.1.0
isort>=5.12.0
mypy>=1.5.0
pre-commit>=3.3.0

# Utilities
scipy>=1.11.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Performance and optimization
onnx>=1.14.0
onnxruntime>=1.15.0
onnxruntime-gpu>=1.15.0  # Optional: For GPU inference
psutil>=5.9.0

# Caching
redis>=4.6.0  # Optional: For Redis caching

# 2025+ Cutting-Edge Features

# Diffusion models
diffusers>=0.25.0
transformers>=4.36.0
accelerate>=0.25.0

# Vision Transformers
timm>=0.9.12

# Web UI
gradio>=4.13.0

# Mobile deployment
tensorflow>=2.15.0  # For TFLite conversion
onnx-tf>=1.10.0
tf2onnx>=1.15.0

# Privacy-preserving ML
opacus>=1.4.0  # Differential privacy
tenseal>=0.3.14  # Homomorphic encryption

# 3D reconstruction
trimesh>=4.0.0  # For mesh processing
pyrender>=0.1.45  # For rendering (optional)

# Next-generation features (2025+)

# NeRF and 3D rendering
# nerfacc>=0.5.0  # For NeRF acceleration (optional)
# tiny-cuda-nn>=1.7  # For Instant-NGP (optional, requires CUDA)

# Segment Anything Model
# segment-anything>=1.0  # Install from: pip install git+https://github.com/facebookresearch/segment-anything.git

# CLIP for text-guided enhancement
# clip @ git+https://github.com/openai/CLIP.git  # OpenAI CLIP

# Explainable AI
lime>=0.2.0  # LIME for local explanations
shap>=0.43.0  # SHAP for feature importance
captum>=0.6.0  # PyTorch interpretability library

# Enterprise Features (Commercial)

# Billing integration
stripe>=7.0.0  # For Stripe payment processing (optional)
# boto3>=1.34.0  # For AWS Marketplace integration (optional)

# Database for multi-tenancy (choose one)
# psycopg2-binary>=2.9.0  # PostgreSQL
# pymongo>=4.6.0  # MongoDB

# Authentication (enterprise)
# python-saml>=1.15.0  # SAML SSO support
# python-jose>=3.3.0  # JWT tokens
# passlib>=1.7.4  # Password hashing

# Advanced dependencies (optional)
# Install based on your needs:
# - For full diffusion support: pip install diffusers[torch] transformers accelerate
# - For privacy: pip install opacus tenseal
# - For mobile: pip install tensorflow onnx-tf
# - For 3D: pip install trimesh pyrender
# - For NeRF: pip install nerfacc tiny-cuda-nn
# - For SAM: pip install git+https://github.com/facebookresearch/segment-anything.git
# - For CLIP: pip install git+https://github.com/openai/CLIP.git
# - For XAI: pip install lime shap captum
# - For Enterprise: pip install stripe boto3 psycopg2-binary python-saml
